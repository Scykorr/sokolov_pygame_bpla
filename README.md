# Quad-Copter vs Missile Simulation

Интерактивная симуляция боевого взаимодействия между ракетой и квадрокоптером с GUI на pygame.

## Описание

**Дрон в центре** защищается от **ракеты, атакующей с края карты**. Симуляция показывает:
- Ракета спавнится на случайном краю карты и летит к дрону
- Дрон остается в центре (8, 6) км и начинает маневрировать только когда ракета в зоне обнаружения
- Если расстояние между ними становится < 100 метров → **ВЗРЫВ** (ракета перехвачена)
- Если ракета покидает зону досягаемости → **РАКЕТА УШЛА** (дрон не перехвачен)

## Требования

- Python 3.13+
- pygame 2.4.0+
- Pillow 10.0.0+ (для загрузки спрайтов)

## Установка

1. **Активировать виртуальное окружение:**
   ```bash
   .\venv\Scripts\activate
   ```

2. **Установить зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Генерирование спрайтов (опционально):**
   ```bash
   python generate_sprites.py
   ```
   Если спрайты не найдены, программа будет использовать геометрические фигуры.

## Запуск

```bash
python main.py
```

## Интерфейс программы

### Экран настройки
При запуске программы вам будут доступны три поля ввода:
1. **Скорость дрона (км/ч)** - скорость манёвра при убегании от ракеты (типовое значение 80 км/ч)
2. **Скорость ракеты (км/ч)** - скорость атакующей ракеты (типовое значение 120 км/ч)
3. **Радиус зоны (км)** - зона обнаружения, при входе в которую дрон начинает маневрировать

Нажмите **▶️ Старт**, чтобы запустить симуляцию.

### Экран симуляции
Во время работы симуляции доступны кнопки:
- **⏸ Пауза** - приостановить/возобновить симуляцию
- **🔄 Сброс** - вернуться к экрану настройки для новой симуляции

### Элементы на поле боя

**Основные объекты:**
- **🟦 Синий квадрат** (8, 6) км - Квадрокоптер (ЗАЩИТА), неподвижен пока ракета далеко, активно маневрирует в зоне опасности
- **🔴 Красный треугольник** - Ракета (АТАКА), спавнится на краю карты, летит к дрону с нарушениями курса
- **🟢 Зелёная окружность** - Радиус зоны обнаружения дрона (граница активации маневров)
- **💥 Взрыв** - Эффект перехвата (жёлто-оранжевые круги расширяются)

**Статистика в левом углу:**
- ⏱ Время симуляции в секундах
- 🚁 Расстояние, пройденное дроном при маневрировании
- 🚀 Расстояние, пройденное ракетой с момента спавна
- 📏 Текущее расстояние между дроном и ракетой

## Логика симуляции

1. **Инициализация:** Дрон размещается в центре карты (8, 6) км. Ракета спавнится на случайном краю карты.

2. **Полёт ракеты:** Ракета летит к дрону в начальном направлении, но каждые 2-6 секунд меняет курс на ±30 градусов (попытка уклониться).

3. **Обнаружение дрона:** Когда ракета входит в **зелёную зону обнаружения** вокруг дрона, тот активирует режим защиты.

4. **Манёвры дрона:** Дрон начинает двигаться ПРОЧЬ от ракеты со своей скоростью. Успешность защиты зависит от соотношения скоростей и величины зоны.

5. **Перехват:** Если расстояние между ракетой и дроном становится < 100 метров в любой момент → **Взрыв!** (дрон перехваченет)

6. **Или побег:** Если ракета покидает область поиска (уходит слишком далеко за край карты) → **Симуляция завершена** (ракета не перехвачена)

## Параметры карты

- **Размер:** 16 км (ширина) × 12 км (высота)
- **Дрон:** стартует и остаётся в центре (8, 6) км до активации
- **Масштаб:** 1 км = 45 пикселей на экране
- **Дисплей:** 720 × 540 пиксели

## Проект использует

- **pygame** - для отрисовки и обработки событий
- **Python 3.13** - базовый интерпретатор

## Автор

Артем Соколов
